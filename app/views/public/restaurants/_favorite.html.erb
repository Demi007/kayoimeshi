  <% if restaurant.favorited_by?(current_user) %>
     <%= link_to "お気に入り解除", restaurant_favorite_path(restaurant_id: restaurant.id, id: Favorite.find_by(user_id: current_user.id, restaurant_id: restaurant.id).id), method: :delete, remote: true %>
  <% else %>
      <%= link_to "お気に入り登録", restaurant_favorites_path(restaurant.id), method: :post, remote: true %>
  <% end %>