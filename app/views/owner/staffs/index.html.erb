<h1>スタッフ一覧</h1>
　<% @staffs.each do |staff| %>
　  　<%= staff.nick_name %>
　  　<%= staff.introduction%>
　　<%= link_to "編集する", edit_owner_restaurant_staff_path(@restaurant.id, staff.id) %>
　<% end %>
　<div class="btn btn__green">
　 <%= link_to "追加", new_owner_restaurant_staff_path(@restaurant.id), class:"btn"%>
　</div>